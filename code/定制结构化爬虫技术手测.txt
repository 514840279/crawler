test_stru.py #调用测试案例
z_code_0.py #爬虫代码手在一定规则框架下的自由发挥的方法库文件，必须被test_stru无泄露安全import，且结果准确

data\test.html 处理代码标准测试文件 

最后说明：

此文件夹为规范实例，整体上代码手也可以自由发挥，但是不建议改编大的架构随便写。
即：就是 fun_x 方法函数，自由发挥。 run_it 中 print位置自由发挥 。

最重要规约三章：

一、输入必须能接受字符型html原代码 原生代码 不是源代码
二、返回必须是字典类型，且符合{特征值名：抽取结果规范}
三、引用主函数必须是 run_it(*args,**kwargs): 一个字都不能差
